const selectedTheme = localStorage.getItem("selected-theme"); function scrollHeader() { const e = document.querySelector(".header"); 80 <= this.scrollY ? e.classList.add("scroll-header") : e.classList.remove("scroll-header") } selectedTheme && document.body.classList["dark" === selectedTheme ? "add" : "remove"]("dark-theme"), window.addEventListener("scroll", scrollHeader);